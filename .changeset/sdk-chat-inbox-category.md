---
'@audius/sdk': minor
---

Add `chats.setCategory` (RPC `chat.set_category`) to file a chat into the Priority or General inbox, `chats.getUnreadCountByCategory` for per-tab unread counts, the `ChatCategory` enum, and a `category` field on `UserChat`.
