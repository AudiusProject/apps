---
'@audius/spl': minor
---

Add Claimable Tokens `SetAuthority` and `Close` instruction support: `createSetAuthorityInstruction`, `createSignedSetAuthorityData`, `decodeSignedSetAuthorityData`, decoders, and `ClaimableTokensProgram.rentDestination`.
